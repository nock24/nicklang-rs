fn main() {
    let s: str = "hello";
    print(s);
    exit(0);
}
/*
fn fib(i: int) {
    if i {
        if i - 1 {
            return fib(i - 1) + fib(i - 2);
        }
        return 1;
    }
    return 0;
}
*/
